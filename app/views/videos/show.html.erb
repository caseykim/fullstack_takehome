<h2>
  <%= @video.title %>
</h2>
<h4>
  Viewed by <%= @video.users.count %> users
</h4>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= user.name %>
    </li>
  <% end %>
</ul>

<div class="page-nav">
  <%= paginate @users %>
</div>
